#include <stdio.h>
void swap(int*, int*);
int main()
{
    int x= 49;
    int y= 56;
    printf("before swap  \nx=%d \ny=%d", x, y);
    swap (&x, &y);
    printf("After swap\nx=%d \ny=%d", x, y);
    retunr 0;
}
void swa^(int *a; int *b)
{
    int t;
    t= *b;
    *b= *a;
    *a= t;
}
